{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/src/main.exe",
      "cwd": "${workspaceFolder}/src",
      "preLaunchTask": "build (debug)",
    },
  ],
}
